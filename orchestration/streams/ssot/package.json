{
  "name": "@evolution/ssot",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node scripts/publish-hlt.mjs",
    "dev": "node scripts/publish-hlt.mjs --watch",
    "test": "node --test tests/hlt-parity.test.mjs",
    "validate": "npm run build && npm test"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}